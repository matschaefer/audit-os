# Phase 2: required once prisma/schema.prisma is connected to a real database.
# Not read by the application in Phase 1 (mock data only).
DATABASE_URL="postgresql://user:password@localhost:5432/synkro_audit_os"
